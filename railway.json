{ 
  "$schema": "https://railway.app/railway.schema.json", 
  "build": { "builder": "NIXPACKS", 
             "buildCommand": "npm install --include=dev && npx prisma generate && npm run build" 
           }, 
  "deploy": {  "startCommand": "npx prisma migrate resolve --rolled-back 20260814095900_add_super_admin_role && npx prisma migrate resolve --rolled-back 20260814100000_add_password_auth && npx prisma migrate deploy && npm run start:prod", 
              "restartPolicyType": "ON_FAILURE" } 
}
